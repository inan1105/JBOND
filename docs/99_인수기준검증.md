# 최종 인수기준 검증 (Prompt99)

> 상태 범례: ✅ 검증완료 · 🟢 구현완료(프리뷰로 육안확인) · ⏳ 클린 설치 후 자동검증 대기

| # | 인수 조건 | 구현 위치 | 상태 |
|---|---|---|---|
| 1 | 종목명·코드·ISIN 검색 | `SearchScreen` + `dataService.search` | 🟢 |
| 2 | 할인·이표·단리·복리 가격 계산 | `bond-engine/priceBond` (+26 테스트) | ✅ |
| 3 | 10,000원당 Clean·Dirty·경과이자 | `priceBond` 출력, `IssueScreen` 평가정보 | ✅ |
| 4 | 일별 시가평가수익률·가격 조회 | `DistributionScreen` + `PriceYieldChart` | 🟢 |
| 5 | 현재·과거 수익률곡선 비교 | `CurveScreen` (compare/change/spread) | 🟢 |
| 6 | 시가표 헤더·첫열 모바일 고정 | `MtmScreen` sticky + E2E 360/390/430 | 🟢 |
| 7 | 시가표 셀 → 곡선·시뮬레이션 전달 | `MtmScreen` BottomSheet + BondContext | 🟢 |
| 8 | A/B Income·Capital·TR·CAGR | `bond-engine/simulate` (+테스트), `SimulationScreen` | ✅ |
| 9 | 현금흐름·할인과정 조회 | `SimulationScreen` 상세, `IssueScreen` 현금흐름 | ✅ |
| 10 | 기준일·출처·실제/추정 구분 표시 | `ValueTypeBadge/SourceBadge/QualityBadge` 전 화면 | 🟢 |
| 11 | 원천 장애 시 마지막 정상+갱신시각 | `readLastGood` + `StaleBanner` + SW NetworkFirst | 🟢 |

## 검증 방법별 현황
- **✅ 계산 엔진**: `packages/bond-engine` 은 외부 의존이 없어 `vitest run` 으로 독립 검증 완료 — **26/26 통과**.
  - `pricing.test.ts` (유형별 정상·경계·오류), `simulation.test.ts` (A/B·민감도 단조성)
- **🟢 UI 5개 화면**: 전체 구현 완료. `docs/ui-preview.html` (실제 엔진 로직·Mock 데이터 포팅)로 5개 화면·연결·sticky·배지·차트 **육안 검증 완료**.
- **⏳ 자동 E2E/빌드**: `npm install` 이 이 환경의 AV 파일잠금(`EBUSY`)으로 미완료.
  클린 설치(README 트러블슈팅) 후 아래가 그대로 통과하도록 준비됨:
  - `e2e/connections.spec.ts` — 발행→유통, 발행→시뮬, 유통선택일→시뮬, 시가표→곡선, 시가표→시뮬
  - `e2e/mtm-sticky.spec.ts` — 시가표 sticky 360/390/430

## 미완료 항목 / 잔여 작업
1. **`npm install` 완료** — AV 예외 등록 또는 클린 재설치 필요(README 참조). 코드 문제 아님.
2. `npm run build` (타입체크 + Vite PWA) 및 `npm run test:e2e` 자동 실행 — 설치 완료 후 즉시 가능.
3. SEIBro/KOFIA/KRX 실원천 어댑터 — 요청 규격 확인 후 TODO 대체(현재 Mock 격리).
4. PWA 아이콘 PNG 세트(현재 SVG), DB 영속화·배치 활성화 — 실원천 연동 단계.
